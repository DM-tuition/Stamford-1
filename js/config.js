/* ============================================================
   STAMFORD HOUSE — SITE CONFIG
   Edit the two values below to switch features "live".
   (Nothing else in the codebase needs touching.)
   ============================================================ */
window.STAMFORD_CONFIG = {

  // 1) LIVE HOUSE POINTS  ------------------------------------
  // In a Google Sheet with two columns  House | Points
  // (rows: Stamford, Bradbury, Massey, Tatton), go to:
  //   File -> Share -> Publish to web -> (sheet) -> CSV -> Publish
  // Paste that .csv link here. Leave "" to show placeholder standings.
  scoresCsvUrl: "",

  // 2) CONTACT / SIGN-UP FORM  -------------------------------
  // Create a free form at https://formspree.io and paste its endpoint
  // here, e.g. "https://formspree.io/f/abcd1234".
  // Leave "" for demo mode (form shows a thank-you but doesn't send).
  formEndpoint: ""

};
